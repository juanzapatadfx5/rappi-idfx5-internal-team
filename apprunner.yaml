version: 1.0
build:
  commands:
    - echo "Building Docker image..."
    - docker build -t ${AWS_APP_RUNNER_SERVICE_NAME} .
run:
  runtime: python3
  command: docker run -p 8080:8080 ${AWS_APP_RUNNER_SERVICE_NAME}