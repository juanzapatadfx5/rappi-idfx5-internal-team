version: 1.0
runtime: python3

build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  # CRÍTICO: Cambiar 'main:app' a 'app.main:app' para reflejar la estructura de directorios
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
